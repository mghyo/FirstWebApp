'use strict';

const config = {
  googleApiToken: '<redacted>',
  port: 8000,
  database: {
    user: 'student',
    host: 'localhost',
    name: 'postgres',
    password: 'cs6310',
    port: 5432
  }
};

module.exports = config;
